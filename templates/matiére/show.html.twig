{% extends 'base.html.twig' %}

{% block title %}Matiére{% endblock %}

{% block body %}
    <h1>Matiére</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mati_re.id }}</td>
            </tr>
            <tr>
                <th>Id_M</th>
                <td>{{ mati_re.idM }}</td>
            </tr>
            <tr>
                <th>Intitulé</th>
                <td>{{ mati_re.intitulé }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mati_re_index') }}">back to list</a>

    <a href="{{ path('app_mati_re_edit', {'id': mati_re.id}) }}">edit</a>

    {{ include('matiére/_delete_form.html.twig') }}
{% endblock %}
