<form method="post" action="{{ path('app_mati_re_delete', {'id': mati_re.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mati_re.id) }}">
    <button class="btn">Delete</button>
</form>
